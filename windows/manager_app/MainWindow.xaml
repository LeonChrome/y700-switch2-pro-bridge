<Window x:Class="Y700Switch2Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ESP32-S3 Pro2 Bridge" Height="860" Width="1260"
        MinHeight="760" MinWidth="1120"
        Background="#f3f5f7">
    <Window.Resources>
        <Style x:Key="Section" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#d8dee7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#667085"/>
            <Setter Property="Margin" Value="0,4,8,0"/>
        </Style>
        <Style x:Key="Value" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#182230"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinWidth" Value="78"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="9,5"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#17202a" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Foreground="White" FontSize="20" FontWeight="SemiBold" Text="ESP32-S3 Pro2 Bridge"/>
                    <TextBlock Foreground="#cbd5e1" Text="Steam Switch Pro/Pro2 input and rumble control console"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Fill="{Binding ConnectionLampBrush}" Margin="0,0,8,0"/>
                    <TextBlock Foreground="White" Text="{Binding ConnectionStatus}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource Section}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="USB 端状态"/>
                            <TextBlock Text="{Binding UsbIdentityText}" TextWrapping="Wrap"/>
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0" Width="11" Height="11" Fill="{Binding UsbLampBrush}" Margin="0,7,8,0"/>
                                <TextBlock Grid.Column="1" Style="{StaticResource Value}" Text="{Binding UsbStatus, StringFormat=HID: {0}}"/>
                                <Ellipse Grid.Column="0" Width="11" Height="11" Fill="{Binding BulkLampBrush}" Margin="0,31,8,0"/>
                                <TextBlock Grid.Column="1" Style="{StaticResource Value}" Margin="0,28,0,0" Text="{Binding BulkStatus, StringFormat=Bulk: {0}}"/>
                            </Grid>
                            <UniformGrid Columns="2" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource Label}" Text="Steam guard"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding HidGuardStatus}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Bulk RX/TX"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BulkCountsText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="HID OUT"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding HidOutText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="HID GET"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding HidGetText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Bulk detail"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BulkDetailText}" TextWrapping="Wrap"/>
                                <TextBlock Style="{StaticResource Label}" Text="Mode"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding DeviceMode}"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Section}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="USB 端识别状态"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="{Binding BulkLampBrush}" Margin="0,4,8,0"/>
                                <TextBlock Text="{Binding UsbRecognitionText}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <WrapPanel Margin="0,10,0,0">
                                <Button Style="{StaticResource SmallButton}" Content="joy.cpl" Command="{Binding OpenJoyCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="设备管理器" Command="{Binding OpenDeviceManagerCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="状态" Command="{Binding StatusCommand}"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Section}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="BLE 连接"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="14" Height="14" Fill="{Binding BleLampBrush}" Margin="0,4,8,0"/>
                                <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding BleStatus}"/>
                            </StackPanel>
                            <UniformGrid Columns="2" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource Label}" Text="Auto"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BleAutoStatus}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Target"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BleTarget}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Live"/>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="{Binding LiveLampBrush}" Margin="0,7,6,0"/>
                                    <TextBlock Style="{StaticResource Value}" Text="{Binding LiveStatus}"/>
                                </StackPanel>
                                <TextBlock Style="{StaticResource Label}" Text="Updates"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding LiveUpdates}"/>
                            </UniformGrid>
                            <TextBox Text="{Binding BleConnectTarget, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,4" ToolTip="Address, #scan index, name, or empty for last"/>
                            <ComboBox ItemsSource="{Binding BleCandidates}" SelectedItem="{Binding SelectedBleCandidate}" Margin="0,0,0,4" ToolTip="Scanned BLE candidates; 推荐 means it looks like Switch Pro / Pro2"/>
                            <TextBlock Foreground="#5f6b75" Text="{Binding BleSearchStatus}" TextWrapping="Wrap"/>
                            <WrapPanel>
                                <Button Style="{StaticResource SmallButton}" Content="搜索手柄" Command="{Binding BleScanCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="读取列表" Command="{Binding BleListCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="连接选中" Command="{Binding BleConnectSelectedCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="重连" Command="{Binding BleReconnectCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="连接目标" Command="{Binding BleConnectCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="断开" Command="{Binding BleDisconnectCommand}"/>
                            </WrapPanel>
                            <WrapPanel>
                                <Button Style="{StaticResource SmallButton}" Content="上次地址自动开" Command="{Binding BleAutoOnCommand}" ToolTip="保存开启，并立即尝试重连上一次识别到的 BLE 地址"/>
                                <Button Style="{StaticResource SmallButton}" Content="上次地址自动关" Command="{Binding BleAutoOffCommand}" ToolTip="保存关闭；不会断开当前 BLE 连接"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Section}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="测试按钮"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="{Binding RumbleLampBrush}" Margin="0,5,8,0"/>
                                <TextBlock FontWeight="SemiBold" Text="{Binding RumbleStatus, StringFormat=Rumble: {0}}"/>
                            </StackPanel>
                            <WrapPanel Margin="0,8,0,0">
                                <Button Style="{StaticResource SmallButton}" Content="短震" Command="{Binding RumbleShortCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="持续震" Command="{Binding RumbleHoldCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="停止" Command="{Binding RumbleStopCommand}"/>
                            </WrapPanel>
                            <UniformGrid Columns="2" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource Label}" Text="Writes"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding RumbleWrites}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Errors"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding RumbleErrors}"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource Section}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="中控"/>
                        <TextBlock FontWeight="SemiBold" Text="All-in-one 固件刷录"/>
                        <TextBlock Foreground="#475467" Text="{Binding EmbeddedFirmwareText}" TextWrapping="Wrap"/>
                        <TextBlock Foreground="#475467" Text="{Binding DriverStatusText}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                        <TextBlock Foreground="#475467" Text="{Binding SelectedPortDetails}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                        <WrapPanel Margin="0,8,0,4">
                            <Button Style="{StaticResource SmallButton}" Content="{Binding FlashButtonText}" Command="{Binding FlashFirmwareCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="修复重刷" Command="{Binding RepairFlashCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="仅清除" Command="{Binding EraseFirmwareCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="清除并重刷" Command="{Binding EraseAndFlashCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="CH343 驱动" Command="{Binding OpenCh343DriverCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="CH341 驱动" Command="{Binding OpenCh341DriverCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="本地驱动包" Command="{Binding OpenLocalDriverFolderCommand}"/>
                        </WrapPanel>
                        <TextBlock Foreground="#7a2e0e" Text="{Binding FlashStatus}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                        <Separator Margin="0,2,0,8"/>
                        <TextBlock FontWeight="SemiBold" Text="运行控制"/>
                        <WrapPanel>
                            <ComboBox ItemsSource="{Binding Ports}" SelectedItem="{Binding SelectedPort}" Width="120"/>
                            <Button Style="{StaticResource SmallButton}" Content="刷新" Command="{Binding RefreshPortsCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="连接" Command="{Binding ConnectCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="断开串口" Command="{Binding DisconnectCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="开始桥接" Command="{Binding StartCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="停止桥接" Command="{Binding StopCommand}"/>
                        </WrapPanel>
                        <TextBlock Margin="4,2,0,0" Foreground="#5f6b75" Text="{Binding PortHint}" TextWrapping="Wrap"/>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="回报率"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding ReportRatePresets}" SelectedItem="{Binding ReportRateHz}" Width="90"/>
                            <Button Grid.Column="2" Style="{StaticResource SmallButton}" Content="应用 Hz" Command="{Binding SetRateCommand}"/>
                            <WrapPanel Grid.Column="3">
                                <Button Style="{StaticResource SmallButton}" Content="60" Command="{Binding Rate60Command}"/>
                                <Button Style="{StaticResource SmallButton}" Content="125 稳定" Command="{Binding Rate125Command}"/>
                                <Button Style="{StaticResource SmallButton}" Content="250 陀螺仪推荐" Command="{Binding Rate250Command}"/>
                                <Button Style="{StaticResource SmallButton}" Content="500" Command="{Binding Rate500Command}"/>
                                <Button Style="{StaticResource SmallButton}" Content="1000 实验" Command="{Binding Rate1000Command}"/>
                            </WrapPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource Section}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource SectionTitle}" Text="关键参数"/>
                            <UniformGrid Columns="2">
                                <TextBlock Style="{StaticResource Label}" Text="Firmware"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding FirmwareVersion}"/>
                                <TextBlock Style="{StaticResource Label}" Text="HID loop"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding HidStatus}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Report rate"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding ReportRateHz, StringFormat={}{0} Hz}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Actual rate"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding ReportActualText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="BLE input Hz"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BleInputActualText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="BLE interval"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BleConnIntervalText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="BLE gap last/max"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding BleInputGapText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Report sent/fail"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding ReportCounterText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Gap last/max"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding ReportGapText}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Live age"/>
                                <TextBlock Style="{StaticResource Value}" Text="{Binding LiveAgeMs, StringFormat={}{0} ms}"/>
                            </UniformGrid>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource SectionTitle}" Text="震动参数"/>
                            <UniformGrid Columns="4">
                                <TextBlock Style="{StaticResource Label}" Text="Scale"/>
                                <TextBox Text="{Binding RumbleScalePercent, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Hold"/>
                                <TextBox Text="{Binding RumbleHoldMs, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Tick"/>
                                <TextBox Text="{Binding RumbleTickMs, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource Label}" Text="Stop"/>
                                <TextBox Text="{Binding RumbleStopPackets, UpdateSourceTrigger=PropertyChanged}"/>
                            </UniformGrid>
                            <WrapPanel>
                                <Button Style="{StaticResource SmallButton}" Content="读取" Command="{Binding RumbleConfigCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="应用" Command="{Binding ApplyRumbleTuneCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="Info 日志" Command="{Binding LogLevelInfoCommand}"/>
                                <Button Style="{StaticResource SmallButton}" Content="Debug 日志" Command="{Binding LogLevelDebugCommand}"/>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource Section}" Margin="0">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SectionTitle}" Text="日志" VerticalAlignment="Center"/>
                            <Button Style="{StaticResource SmallButton}" Content="清空" Command="{Binding ClearLogsCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="保存" Command="{Binding SaveLogsCommand}"/>
                            <Button Style="{StaticResource SmallButton}" Content="复制" Command="{Binding CopyLogsCommand}"/>
                            <TextBox Width="260" Text="{Binding CustomCommand, UpdateSourceTrigger=PropertyChanged}" ToolTip="Raw serial command"/>
                            <Button Style="{StaticResource SmallButton}" Content="发送" Command="{Binding SendCustomCommandCommand}"/>
                        </StackPanel>
                        <TextBox x:Name="LogTextBox"
                                 Text="{Binding LogText, Mode=OneWay}"
                                 TextChanged="LogTextBox_TextChanged"
                                 IsReadOnly="True"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 TextWrapping="NoWrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"/>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
